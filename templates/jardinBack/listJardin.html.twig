{% extends 'admin/indexAdmin2.html.twig' %}

{% block title %}Liste des Jardins - Back{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="icon" type="image/x-icon" href="{{ asset('assets/img/logo.png') }}" />
    <link href="{{ asset('https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/style.min.css') }}" rel="stylesheet" />
    <link href="{{ asset('assets_back/css/styles.css') }}" rel="stylesheet" />
    <script src="{{ asset('https://use.fontawesome.com/releases/v6.3.0/js/all.js') }}" crossorigin="anonymous"></script>
{% endblock %}

{% block content %}
<div class="container">
    <h2 class="text-rose">Liste des Jardins</h2>
    <a href="{{ path('app_addJardinBack') }}" class="btn btn-success mb-3">Ajouter un Jardin</a>
    <table class="table">
        <thead>
    <tr>
        <th>Lieu</th>
        <th>Taille</th>
        <th>Actions</th>
    </tr>
</thead>

        <tbody>
    {% for j in jardin %}
        <tr>
            <td>{{ j.lieu }}</td>
            <td>{{ j.taille }}</td>
            <td>
                <a href="{{ path('app_delete_jardinB', {'id': j.id}) }}" class="btn btn-danger">Supprimer</a>
                 <a href="{{ path('app_update_jardinB', {'id': j.id}) }}" class="btn btn-danger">Mettre à jour</a>
            </td>
        </tr>
    {% endfor %}
</tbody>

    </table>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/bootstrap.bundle.min.js') }}" crossorigin="anonymous"></script>
    <script src="{{ asset('js/scripts.js') }}"></script>
    <script src="{{ asset('js/simple-datatables.min.js') }}" crossorigin="anonymous"></script>
    <script src="{{ asset('js/datatables-simple-demo.js') }}"></script>
{% endblock %}
